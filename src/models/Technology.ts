export class Technology {
type: number; // Software, Other
name: string;
level: number;

    constructor(type: number, name: string, level: number) {
    this.type = type;
    this.name = name;
    this.level = level;
    }
}